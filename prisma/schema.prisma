generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model Tour {
  id          Int    @id @default(autoincrement())
  name        String
  description String
  duration    String
  difficulty  String
  location    String
  altitude    String
  image       String
}